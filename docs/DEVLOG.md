# Дневник разработки "Пробибику" 🚗

Этот файл ведется ИИ-ассистентом параллельно с разработкой проекта. Здесь фиксируются ключевые решения, архитектурные сдвиги и продуктовые инсайты.

---

## [2026-04-10] Этап 0: Фундамент и Видение

### 🎯 Цель этапа
Сформулировать концепцию MVP и определить "Скучную Архитектуру" (Boring Architecture), которая выдержит рост до 1 млн пользователей.

### ✅ Что сделано
- Изучен контекст: Сервис для мамы, который должен превратиться в масштабируемый продукт.
- Выбран стек: **Next.js (App Router)** + **TypeScript** + **PostgreSQL** + **Drizzle ORM**.
- Определена инфраструктура: **Yandex Cloud** (Compute, S3, Vision OCR, YandexGPT).
- Спроектирована схема БД: Акцент на `service_records` и `work_items` с поддержкой JSONB для сырых данных OCR.

### 🏗 Архитектурные решения
1. **Выбор Drizzle ORM вместо Prisma**: Для максимального контроля над SQL и производительности, что критично при планах на 1 млн юзеров.
2. **Гибридный OCR**: Использование Yandex Vision для текста + YandexGPT для превращения "каши" из слов в структурированный JSON. Это дешевле и гибче, чем готовые сервисы парсинга чеков.
3. **Хранение в S3**: Оригиналы накладных — это юридическая и историческая ценность, поэтому сразу заложили S3 с политикой жизненного цикла (Cold Storage).

### 💡 Инсайты
- Проект вдохновлен реальной болью (папка с накладными за 10 лет). Это дает четкий критерий успеха: "Сможет ли мама этим пользоваться?".
- Главный риск — качество распознавания старых или мятых накладных. Поэтому в схему БД заложен статус `manual` для ручной корректировки.

### 🏁 Что дальше?
Переход к реализации базового каркаса приложения и настройки окружения.

---

## [2026-04-10] Этап 1: Детализация Беклога и Приоритизация

### 🎯 Цель этапа
Превратить общие идеи в конкретный список задач для разработки.

### ✅ Что сделано
- Переработан `research/product_backlog.md`.
- Выделены 4 ключевых Milestone: от фундамента до масштабирования.
- Установлены жесткие приоритеты P0 для "Ядра продукта" (OCR Flow).

### 🏗 Архитектурные решения
1. **Milestone-driven development**: Сначала строим инфраструктуру (M0), затем сразу переходим к самой сложной части — связке OCR + LLM (M1), чтобы как можно раньше проверить главную техническую гипотезу проекта.

### 💡 Инсайты
- Мы решили не тратить время на "красивые графики" (Milestone 3) на старте. Самая большая ценность — это просто перестать копить бумажки и оцифровать их. Поэтому "Timeline" и "OCR Flow" — наши главные приоритеты.

### 🏁 Что дальше?
Инициализация проекта: Next.js + Tailwind + Drizzle. Настройка связи с базой данных.

---

## [2026-04-10] Этап 2: Milestone 0 — Основание (Infrastructure & Auth)

### 🎯 Цель этапа
Создать технический скелет приложения: настроить Next.js, типизацию, базу данных и аутентификацию.

### ✅ Что сделано
- **Инициализация проекта**: Создано Next.js приложение (App Router, TS, Tailwind) в корне репозитория.
- **База данных (Drizzle ORM)**:
  - Спроектирована и описана схема таблиц: `users`, `accounts`, `sessions`, `verificationTokens` (NextAuth), `vehicles`, `service_records`, `work_items`.
  - Настроен `drizzle.config.ts` и `src/db/index.ts`.
  - Добавлены скрипты для генерации и пуша миграций в `package.json`.
- **Аутентификация (NextAuth)**:
  - Настроен Google Provider.
  - Интегрирован `DrizzleAdapter` для автоматического сохранения пользователей в БД.
  - Создан клиентский компонент `LoginButton` и обертка `NextAuthProvider`.
- **Облачное хранилище (Yandex S3)**:
  - Настроен S3-клиент в `src/lib/s3.ts`.
  - Реализована функция генерации presigned-ссылок для безопасной загрузки файлов напрямую с клиента.
- **Интерфейс**:
  - Создана базовая Landing-страница (M0) с прицелом на брендинг "Пробибику.ру".

### 🏗 Архитектурные решения
1. **Использование UUID для всех сущностей**: Обеспечивает легкое шардирование и безопасность (ID нельзя угадать) для масштабирования до 1 млн пользователей.
2. **Presigned URLs для S3**: Минимизирует нагрузку на сервер Next.js — файлы летят напрямую в S3, сервер лишь выдает временные разрешения.
3. **Строгая типизация в Drizzle**: Используем `$type` для JSONB колонок (например, в настройках пользователя), чтобы ИИ-ассистент не ошибался в будущем.

### 🏁 Что дальше?
Переход к Milestone 1: Реализация OCR Core Loop. Загрузка фото -> Распознавание -> Проверка данных.

---

## [2026-04-17] Этап 3: Оживление Интерфейса и Дизайн-система

### 🎯 Цель этапа
Создать визуально богатый, адаптивный и технологичный лендинг, заложить базу для масштабирования интерфейса.

### ✅ Что сделано
- **Дизайн-система и Пространство**:
  - Увеличены вертикальные отступы ("воздух") во всем приложении для создания премиального ощущения.
  - Внедрена поддержка темной темы через `next-themes` и системные переменные Tailwind.
  - Настроены Glassmorphism-эффекты (`glass`) для карточек и навигации.
- **Лендинг**:
  - Реализован блок **"Возможности"** с интерактивными карточками (супер-эллипсы, hover-свечение, вылетающие стрелки).
  - Реализован блок **"Как это работает"** с визуализацией шагов и градиентными индикаторами.
  - Настроена полная мобильная адаптация, включая анимированное бургер-меню на `framer-motion`.
- **Авторизация**:
  - Создана кастомная страница входа (`/auth/signin`) в общем стиле проекта.
  - Интегрирован вход через Яндекс и Magic Link (Email).
- **UX Writing**:
  - Принято и зафиксировано в правилах проекта решение: точки в конце заголовков и коротких абзацев в интерфейсе НЕ ставятся.

### 🏗 Архитектурные решения
1. **Theme-first Development**: Все новые компоненты сразу пишутся с поддержкой темной темы. Это предотвращает накопление "визуального долга".
2. **Custom Auth Pages**: Отказ от дефолтных страниц NextAuth в пользу кастомных позволяет сохранить консистентность бренда "Пробибику" с первого касания пользователя.
3. **Refocused Product Scope**: Решено отказаться от отслеживания расходов на топливо и АЗС (это закрыто банковскими приложениями) в пользу глубокой технической истории обслуживания (запчасти, артикулы, регламенты).

### 💡 Инсайты
- "Воздух" в дизайне — это не пустота, а инструмент управления вниманием. Увеличение отступов до 200px+ на 4K мониторах значительно улучшило восприятие продукта.
- Использование `framer-motion` для микро-взаимодействий (поворот иконки на 3 градуса при наведении) создает ощущение "дорогого" продукта без перегрузки кода.

### 🏁 Что дальше?
Переход к Milestone 1 (M1): Разработка Личного кабинета (Гаража) и интеграция OCR-движка для оцифровки первой накладной.

---

## [2026-04-17] Этап 4: Milestone 1 — Оживление "Ядра" (OCR Core Loop)

### 🎯 Цель этапа
Реализовать полный цикл оцифровки бумажной накладной: от фото до структурированной записи в БД.

### ✅ Что сделано
- **Личный кабинет (Гараж)**:
  - Создана группа роутов `(dashboard)` с защищенным макетом и серверной проверкой сессии.
  - Реализована страница Гаража с динамическим списком автомобилей.
  - Создана форма добавления автомобиля через модальное окно с использованием `framer-motion` для плавности.
- **OCR Engine (Phase 1)**:
  - Реализован API-маршрут `/api/ocr/analyze` с мок-логикой, имитирующей работу Yandex Vision и YandexGPT (задержка 2.5с + структурированный JSON).
  - Настроен API для загрузки файлов в Yandex S3 через presigned-ссылки.
- **OCR UI (Flow)**:
  - Создан многостадийный компонент `OCRModal` (Idle -> Uploading -> Analyzing -> Review -> Saving -> Success).
  - Реализован интерфейс **Review**, где пользователь может редактировать распознанные данные, список работ и запчастей перед сохранением.
- **База данных**:
  - Реализован эндпоинт для сохранения комплексной записи (Service Record + Work Items).
  - Добавлена логика автоматического обновления текущего пробега автомобиля при добавлении новой записи.

### 🏗 Архитектурные решения
1. **Direct-to-S3 Upload**: Файлы загружаются напрямую с клиента в S3, не нагружая сервер Next.js.
2. **Transaction-like Save**: При сохранении записи данные распределяются по таблицам `service_records` и `work_items`, обеспечивая целостность истории.
3. **Mock-first OCR**: Использование мока на этапе M1 позволило полностью отладить UI/UX и схему данных, не дожидаясь настройки ключей Yandex Cloud.

### 💡 Инсайты
- Процесс "ожидания нейросети" (Analyzing) с хорошим спиннером и описанием того, что происходит, значительно повышает доверие пользователя к результату.
- Автоматическое обновление пробега авто из накладной — маленькая, но очень важная деталь для "магического" UX.

### 🏁 Что дальше?
Переход к Milestone 2: Улучшение визуализации истории (Timeline), просмотр оригиналов чеков и детальная статистика расходов.

---

## [2026-04-22] Этап 5: Завершение и полировка Landing Page

### 🎯 Цель этапа
Полностью завершить дизайн главной страницы (Landing Page), реализовать все запланированные смысловые блоки и подготовить продукт к первому показу.

### ✅ Что сделано
- **Разработаны новые компоненты Landing Page**:
  - `Hero` (уже был, но доработан и уплотнен).
  - `Features` (Возможности).
  - `HowItWorks` (Как это работает): исправлены критические проблемы с видимостью текста (адаптация под Tailwind 4), настроены контрастные цвета и анимированные индикаторы шагов.
  - `Security` (Безопасность): создан блок с акцентом на Yandex Cloud, шифрование S3, приватность и бекапы (соответствует архитектуре). Добавлена 3D-анимация сервера.
  - `FAQ` (Частые вопросы): реализован компонент аккордеона на базе `framer-motion` для плавного раскрытия ответов.
  - `Footer`: создан современный подвал сайта с навигацией, юридическими ссылками, контактами (с использованием актуальных иконок из `lucide-react`, заменен `Github` на `LayoutDashboard` из-за версии библиотеки) и индикатором работы систем.
- **Полировка и верстка**:
  - Скорректированы отступы между блоками: вместо огромных `py-32 lg:py-48` установлены сбалансированные `py-20 lg:py-32` (80px - 128px) для создания динамичного и плотного интерфейса.
  - Осуществлен переход на семантические цвета Tailwind (`text-foreground`, `text-muted-foreground`), так как кастомная тема (`@theme`) не содержала дефолтных цветов (например, `slate`), что вызывало "прозрачный" текст в карточках.

### 🏗 Архитектурные решения
1. **Design Tokens First**: Переход от хардкода оттенков (типа `slate-900`) к семантическим переменным (`text-foreground`, `bg-background`). Это обеспечивает идеальную работу темной/светлой темы без дублирования классов.
2. **Component Separation**: Каждый логический блок лендинга (Hero, Features, HowItWorks, Security, FAQ, Footer) вынесен в отдельный компонент для упрощения поддержки и сборки страницы.

### 💡 Инсайты
- В Tailwind 4 механизм `@theme` полностью переписывает дефолтные цвета, если они не импортированы явно. Это вызвало исчезновение текста, но привело к правильному архитектурному решению: использовать только CSS-переменные проекта.
- Динамика (пульсирующие точки в шагах, анимированный сервер в безопасности) и правильные отступы играют ключевую роль в восприятии продукта. 120px между блоками — оптимальный стандарт для современного веба.

### 🏁 Что дальше?
Переход к реализации реального OCR через Yandex Vision (Milestone 1 Core) и улучшению визуализации истории (Timeline) в гараже.

---

## [2026-04-30] Этап 6: Инфраструктурный прорыв и Backend Core

### 🎯 Цель этапа
Оживить "мозг" системы: настроить реальную связь с Yandex Cloud (OCR + GPT + S3) и развернуть постоянную базу данных в Supabase.

### ✅ Что сделано
- **Инфраструктура Yandex Cloud**:
  - Настроены и проверены **API-ключи** для нейросетей и **Статические ключи** для S3.
  - Решена проблема `code 16 (Unknown API Key)` путем активации биллинга и корректного распределения ролей (`ai.vision.user`, `storage.editor`).
  - Успешно протестирована загрузка файлов в Yandex Object Storage (S3).
- **OCR & AI Pipeline**:
  - Реализован и протестирован полный цикл распознавания: **Yandex Vision** (извлечение текста) -> **YandexGPT** (структурирование в JSON).
  - Подтверждено высокое качество распознавания на реальных чеках за период 2014-2025 гг. (включая рукописные пометки и сложные таблицы).
- **База данных (Supabase)**:
  - Описана финальная схема Drizzle: `users`, `vehicles`, `service_records`, `work_items`.
  - Реализована поддержка статусов записей (`draft`, `processed`, `manual`).
  - База успешно развернута в **Supabase**, применены миграции.
  - Проведен успешный тест сохранения: данные из OCR-пайплайна теперь автоматически попадают в таблицы БД.

### 🏗 Архитектурные решения
1. **Separation of Keys**: Использование разных типов ключей (API vs Static) для безопасности и соответствия стандартам Yandex Cloud.
2. **AI-Driven Data Entry**: Вся тяжелая логика парсинга переложена на YandexGPT, что позволяет использовать дешевый тариф Vision OCR (`TEXT_DETECTION`) вместо дорогого табличного.
3. **Master Pipeline Script**: Создан унифицированный скрипт-оркестратор, который объединяет загрузку, распознавание и сохранение в одну атомарную операцию.

### 💡 Инсайты
- Yandex Vision + YandexGPT — это "киллер-фича". Качество разбора чека 10-летней давности с точностью до копейки и артикула запчасти подтверждает жизнеспособность продукта.
- Конфигурация прав в Облаке — это самый сложный этап. "Прозрачность" тарификации и мониторинга в Яндексе помогает контролировать расходы на MVP.

### 🏁 Что дальше?
С бэкенд-ядром покончено. Следующий шаг — интеграция этого ядра во Frontend: создание страницы загрузки чека и ленты истории в Личном кабинете.

---

## [2026-05-12] Этап 7: Перезагрузка UI и интеграция нового фронтенда

### 🎯 Цель этапа
Полностью заменить прототипный фронтенд на финальный дизайнерский лендинг, сохранив при этом всю бэкенд-логику и документацию.

### ✅ Что сделано
- **Полная очистка**: Удалены старые компоненты и страницы (`src/app`, `src/components`), чтобы избежать конфликтов со старым кодом.
- **Интеграция нового дизайна**:
  - Перенесен профессиональный лендинг из Figma-экспорта.
  - Структура адаптирована под **Next.js App Router** (созданы новые `layout.tsx` и `page.tsx`).
  - Все клиентские компоненты помечены директивой `"use client"`.
  - Массово исправлены пути импорта на абсолютные (алиас `@`).
- **Технологическое обновление**:
  - Осуществлен переход на **Tailwind CSS v4** с использованием нового синтаксиса тем (`@theme`).
  - Библиотека анимаций обновлена: заменен `motion/react` на стандартный `framer-motion` для стабильности.
  - Установлены новые зависимости UI-кита (Radix UI, MUI, Lucide-react v0.487).
- **Оптимизация CSS**:
  - Все разрозненные CSS-файлы (темы, шрифты) объединены в единый оптимизированный `src/app/globals.css`.
  - Устранены ошибки парсинга, вызванные конфликтами версий Tailwind и устаревшими импортами.

### 🏗 Архитектурные решения
1. **Surgical Replacement**: Мы не стали "патчить" старый фронтенд, а полностью заменили его слоем представления, сохранив при этом "мозг" системы (`src/db`). Это позволило избежать техдолга на этапе смены дизайна.
2. **Tailwind v4 First**: Проект переведен на самую актуальную версию CSS-фреймворка, что упрощает работу с переменными темы и ускоряет сборку.
3. **Component Encapsulation**: Новые компоненты импортируют свои ресурсы из `src/imports`, что разделяет логику UI и статические ассеты.

### 💡 Инсайты
- Перенос готового фронтенда в существующий проект требует внимательного отношения к версиям React (18 vs 19) и библиотек анимаций. Прямая замена `motion/react` на `framer-motion` — самый простой путь к совместимости.
- Чистый `globals.css` в Tailwind v4 работает лучше, чем россыпь импортов, так как v4 сама эффективно управляет слоями и зависимостями.

### 🏁 Что дальше?
Связка нового UI с реальным бэкендом: подключение формы входа, настройка роутинга в Личный кабинет и запуск OCR Flow внутри нового интерфейса.
